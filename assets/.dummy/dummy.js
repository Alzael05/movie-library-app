
																// var edit_fields = [];

																// $.each( 
																// 		columns, 
																// 		function ( key, val ) 
																// 		{
																// 			// console.log(val);
																// 			if ( val.hasOwnProperty( 'editor' ) )
																// 			{
																// 				edit_fields.push( val.field );
																// 			}
																// 		}
																// 	);

																// console.log( row );
																// console.log( edit_fields );

																											    				// $csrf_hiddn = $form.next( 'input[name="csrf_test_name"]' );

																// $form.html( '<input type="hidden" id="'+row+'">' ).val( val );
																// $form.attr( 'action', '' );

																																			// if ( key[0] )
																			// {
																			// }
																			// $( mdl_element ).find( 'form' ).has( key ).val( val );
																			// console.log( key );
																			// console.log( mdl_element );
																			// console.log( val );
																			// console.log( $( mdl_element ).find( 'form' ).find( '[id="'+key+'"]' ) );

																							    											// $( mdl_element );
				    											// console.log( $form );

												            	// console.log( datas );
												            		// console.log( t_r );
				    											